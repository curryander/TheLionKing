import { Rule } from '@angular-devkit/schematics';
import type { SchemaOptions } from '../../config/schema';
declare const expandabletableSchematic: (options: SchemaOptions) => Rule;
export { expandabletableSchematic };
