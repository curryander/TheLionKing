import { Rule } from '@angular-devkit/schematics';
import type { SchemaOptions } from '../../config/schema';
declare const htmlTableSchematic: (options: SchemaOptions) => Rule;
export { htmlTableSchematic };
